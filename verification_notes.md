# یادداشت بازبینی رابط کاربری

در بازبینی اجرای واقعی Django در تاریخ ۲۲ اوت ۲۰۲۶، صفحه اصلی در مسیر `/` و فهرست شهدا در مسیر `/martyrs/` با عنوان فارسی، جهت RTL، پوسته سبز یشمی/سرمه‌ای/کرم و ناوبری فارسی نمایش داده شدند.

صفحه اصلی شامل معرفی، جست‌وجوی سریع، آمار محتوای زنده، حالت خالی شهدای شاخص و بخش آخرین خاطرات بود. صفحه فهرست، تمامی فیلترهای درخواست‌شده شامل نام، استان، شهر، یگان، بازه تاریخ شهادت، کلیدواژه وصیت‌نامه و نوع خاطره را نمایش داد و در نبود داده، حالت خالی واضح و لینک ورود به مدیریت محتوا ارائه کرد.

بازبینی با داده واقعی انجام نشد؛ پلتفرم عمداً بدون داده ساختگی آماده شده و محتوای واقعی باید از طریق Django Admin وارد شود.

نمای موبایل صفحه اصلی نیز در اندازه ۳۹۰×۸۴۴ بررسی شد. منوی همبرگری، نشانه برند، المان یادمان، تیتر، متن معرفی و فرم جست‌وجو بدون هم‌پوشانی و با ترتیب مناسب موبایل نمایش داده شدند.

در بازبینی نسخه جلالی، کش PWA نسخه پیشین پاک‌سازی شد. راهبرد سرویس‌ورکر نیز به «شبکه در اولویت برای صفحات Django و کش برای حالت آفلاین» تغییر یافت تا پس از هر به‌روزرسانی، داده و قالب تازه نمایش داده شود.

در اجرای واقعی فهرست شهدا، ورودی‌های بازه شهادت با نام‌های جلالی و الگوی `۱۴۰۰-۰۱-۰۱` بارگذاری شدند. جست‌وجوی AJAX نیز با واردکردن عبارت آزمایشی بررسی شد: نشانی صفحه بدون بارگذاری مجدد به پارامتر جست‌وجو تغییر کرد، ناحیه نتایج از حالت مشغول خارج شد و پیام «نتایج بدون بارگذاری مجدد به‌روزرسانی شد» نمایش یافت.

در بازراه‌اندازی کنترل‌شده درگاه ۸۰۰۰، فرایند قبلی Django متوقف و سرور با تنظیمات فعلی دوباره اجرا شد. پاسخ محلی `HTTP 200` و نمایش موفق صفحه اصلی از نشانی عمومی نیز پس از آن تأیید شد.

هدر پس از اصلاح RTL در دسکتاپ و نمای موبایل ۳۹۰×۸۴۴ بررسی شد. برند در سمت راست باقی مانده، منوی همبرگری در سمت چپ قرار گرفته و هیچ هم‌پوشانی در هدر مشاهده نشد.

در آزمون اولیه ورود پنل ادمین از نشانی آزمایشی، محافظت CSRF منشأ پروکسی عمومی را معتبر نمی‌دانست و خطای ۴۰۳ رخ داد. منشأ آزمایشی در تنظیمات قابل‌پیکربندی `CSRF_TRUSTED_ORIGINS` ثبت و سرور برای آزمون مجدد بازراه‌اندازی شد.

پس از اعمال تنظیم CSRF، ورود واقعی با حساب مدیر ایجادشده با موفقیت انجام شد و داشبورد Django Admin شامل شهدا، خاطرات، گالری تصاویر و گالری ویدئوها نمایش داده شد.

فرم افزودن تصویر در پنل ادمین بازبینی شد و جایگاه «پیش‌نمایش زنده» همراه فایل‌های استایل و اسکریپت اختصاصی در فرم نمایش یافت.

با انتخاب یک فایل تصویری آزمایشی در فرم واقعی پنل، جایگاه پیش‌نمایش به عنصر تصویر با نشانی موقت مرورگر تبدیل شد و متن جایگزین «پیش‌نمایش تصویر انتخاب‌شده» نمایش داده شد.

در توقف مجدد سرور، فرایند Django موجود روی درگاه ۸۰۰۰ به‌صورت کنترل‌شده متوقف و دوباره اجرا شد. پاسخ محلی `HTTP 200` و نمایش موفق صفحه اصلی از نشانی عمومی پس از بازراه‌اندازی تأیید شد.

در نسخه PWA، سرویس‌ورکر فعال با محدوده ریشه وب‌سایت تأیید شد و ناوبری پایین اپ‌مانند نیز در DOM صفحه اصلی وجود داشت.

نمای ۳۹۰×۸۴۴ موبایل بررسی شد: هدر فشرده، کارت نصب و نوار ناوبری پایین بدون هم‌پوشانی نمایش یافتند. کش فعال PWA نیز شامل صفحه اصلی، صفحه آفلاین و Manifest است.

برای آزمون آفلاین، سرور موقتاً متوقف شد. پروکسی آزمایشی پیش از رسیدن درخواست به سرویس‌ورکر، صفحه عدم دسترسی محیط را بازگرداند؛ بنابراین امکان تأیید end-to-end از مسیر عمومی وجود نداشت، هرچند منابع اصلی در کش PWA حاضر بودند. سرور بلافاصله بازراه‌اندازی و درگاه عمومی دوباره فعال شد.
